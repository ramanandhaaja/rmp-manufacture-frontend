const PageTitle = ({ title }) => {
  return (
    <div className="border-b border-gray-200 mb-4">
      <h1 className="text-2xl font-semibold text-indigo-900 mb-4">{title}</h1>
    </div>
  );
};

export default PageTitle;
